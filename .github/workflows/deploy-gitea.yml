name: Deploy Gitea (Existing Setup)

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Deploy existing Gitea using Docker Compose
        run: |
          docker compose down || true
          docker compose up -d


